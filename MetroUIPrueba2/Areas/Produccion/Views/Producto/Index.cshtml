@model IEnumerable<MetroUIPrueba2.Models.Producto>
@{
    ViewBag.Title = "Intranet";
    Layout = "~/Areas/Produccion/Views/Shared/_IntranetLayoutInsumos.cshtml";
}

<style>
    #principal {
        width: 900px;
        height: auto;
        top: 50%;
        margin-top: 60px;
        border: 1px solid green;
        padding: 50px;
        position: relative;
    }

    #btn_nuevo {
        margin-bottom: 20px;
        margin-left: 10px;
    }
</style>


<div class="container" id="principal">
    <div style="display:flex; flex-direction:row; align-items:baseline; justify-content:space-between">
        <h3>Lista de Productos</h3>
        <a id="btn_nuevo" href="@Url.Action("Create","Producto")" class="button primary rounded"><span class="mif-add icon">Nuevo</span></a>
    </div>
    <hr />
    <table class="table compact">
        <thead>
            <tr>
                <th>#</th>
                <th>Codigo</th>
                <th>Codigo</th>
                <th>Ingreso</th>
                <th>Valoracion</th>
                <th>Descripción</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (MetroUIPrueba2.Models.Producto p in Model)
            {
                <tr>
                    <td>@p.Id</td>
                    <td>User-01</td>
                    <td>PD-19</td>
                    <td>20-02-2018</td>
                    <td>$ 650.000</td>
                    <td>La descripcion...</td>
                    <td>
                        <a href="@Url.Action("Ver","Producto", new { id = 1 })">Ver/</a>
                        <a href="@Url.Action("Editar", "Producto", new {id=1 })">Editar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>



